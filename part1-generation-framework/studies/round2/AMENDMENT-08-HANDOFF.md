# 接手时的剩余计划：2026-09-08

用户要求先汇报并将全部进度上传，便于其他agent接手。本次不再启动新增付费测试；已在跑的128次表达消融评审完整收尾。所有生成01—09与现有评审均已落盘。

最新表达消融结果不能解释为“必须保留表达块”：删块自动可用率93.75% vs完整92.19%；Luna话术均分持平，其余三维小幅升，Qwen四维点估计均升但缺1份评分，大部分区间含0。下一步要验证它相对原基线的结果，而不是机械保留完整p24。

固定下一轮 `10_wording_followup.json`：64题×中英×p24/删表达块/v3=384次，随机交错，同参数。它尚未调用，也不是最终冻结。完整p24暂作开发参照；删表达块版本尚无自身的完整语言交叉/独立留出，不可沿用p24结论。

剩余资源按当前检查点规划：DeepSeek尚有2893次，最多7000次与10元观测支出上限不变；评审尚有672次、美元总上限2.50不变。建议分配如下，不能把未跑计划写成已完成：

| 工作 | 新DS调用 | 新评审调用 | 进入条件 |
|---|---:|---:|---|
| 八模块体验小样本诊断，已登记ablation-all | 0 | 256 | 可直接续计划；每模块仅4题，不能证明普遍必要性 |
| 表达删块三臂同批对照，计划10 | 384 | 0 | 可直接执行 |
| 下一候选对v3开发盲评，12题中英、AB/BA、两模型 | 0 | 96 | 先固定抽样和计划；不能按结果筛题 |
| 最终候选中/英文指令交叉，64题、中英输入 | 最多256 | 0 | 候选改变时重新翻译校核/登记；若仍p24可引用已跑09 |
| 134题完整开发回归：候选+v3+v0，中英各1遍 | 804 | 0 | 冻结候选、参数和能力哈希后生成新计划；一致性由下项三遍报告 |
| 新留出80题，中英×候选/v3×3遍 | 960 | 0 | 首次调用前冻结选择；之后不得据此继续调prompt |
| 留出体验盲评：固定40题×中英×AB/BA×两模型 | 0 | 320 | 首次评审前按类别固定抽样；所有维度分开、缺失不插补 |
| 预留生成余量 | 489 | 0 | 仅用于登记后的必要复核，不隐式重试失败样本 |

以上新增DS最多2404，连同4107已用为6511，预留489；评审新增672，连同528为1200。若某一步失败，不机械消耗后续确认额度。需要实质改计划时另登记，旧记录不改。全维度门仍未通过；预算约束不构成宣称成功的理由。
