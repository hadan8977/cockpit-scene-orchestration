# 修订 12：p27 收窄两条规则

2026-09-07 UTC 登记，在 p27 的任何生成调用之前写入。

p26 在 11 批 64 题中英同批得可用率 95.31%、安全违规 0、name 与语言合规 100%，高于同批 p24 的 94.53%。逐题看，p26 相对 p24 修好 4 题、新增失败 3 题，两处新增失败都指向修订 10 里我自己加的规则写得太宽：

1. **J05 中英各 1 题**：修订 10 的规则 9 要求「同一语义有 released 条件时不用成熟度更低的条件」，模型据此把「到公司停好车以后提醒我」的触发条件从 `行程事件==到达` 换成 `挡位==挡位P`，与题集金标准冲突。这里有一处已知分歧：Qwen 评审在 N05/J05 上认为 `挡位==挡位P` 比 `行程事件==到达` 更精确，题集金标准要求后者。本轮不改题集金标准，也不删这条冲突记录，改为收窄规则：点名了目的地就用 `行程事件==到达`，只在单说「停好车/熄火后」且没有目的地时才用 `挡位==挡位P`，两者不叠加。
2. **A03 英文 1 题**：修订 10 要求 say 点名一个已写入的动作，模型输出 `Seat at 3, wheel on` 共 19 字符，超过契约的 15 字符。同一条规则在 S08 英文上修好了 p24 原有的 17 字符超限。说明方向没错，缺的是数字符的强制动作。p27 在 say 规则里加入逐字符核对与两个改写示例，并写明宁可少点名一个动作也不能超限。

p27 只改这两处，其余与 p26 逐字一致，哈希记在 `prompts/p27_manifest.json`。

判定：`plans/13_p27_screen.json`，同 64 题、中英、两臂 p27 与 p26，256 次生成。若 p27 的可用率不高于 p26，或出现任何安全违规，停止本条改写线，用 p26 进入完整回归，不做第三次改写。这是本轮登记的最后一次候选迭代。

这两条修改是在开发集上做的，开发集已被反复查看，因此这类逐轮修正存在拟合开发集与拟合评审偏好的风险。控制手段只有一个：最终以未调用过的 80 题留出为准，留出结果无论好坏都按实测报告。
