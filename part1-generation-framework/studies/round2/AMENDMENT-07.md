# 统计、语言与剩余资源登记

在最后留出首次调用之前，新增 `analysis.py`。旧runner和旧score不改；新版语言指标单列，修复旧版“中文总是True”的检查缺口。原题id为聚类单位，保留中英与重复相关性；5000次bootstrap、10000次簇级符号随机化，双侧检验，四体验维度在每个评审内部做Holm校正。生成指标独立成族；p50/p95点估计与均值检验分开，不把均值显著当分位数显著。缺失评分不插补，覆盖不完整就不能宣布已证实全面优于。

已完成的p17/p23结果继续保留原均分及CI，同时补统计推断。开发小样本选出的候选必须在新留出再次确认。全维度要求是验收标准，不能保证一定得到能通过的结果。

后续Prompt语言交叉保留原子能力与示例原文，只逐段等义翻译系统指令；统一中文、统一英文与按输入语言分流比较。如果语义不等价或语言差未证实，报告限制，不沿用第一轮p13结论。

为了完成新增开发候选、消融体验和独立留出，代理自行设定的评审次数上限从600调整为1200，**美元支出上限仍为2.50，账户key限额不变**。这不是用户追加付费额度。DeepSeek仍最多7000次、观测支出10元且余额保底，不使用腾讯端点。启用新次数上限前确认没有另一个评审进程写账本；所有失败请求仍计数，预算不足就停止新增请求并记录未完成项。
