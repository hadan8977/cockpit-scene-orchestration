# 修订 14：候选冻结为 p26

2026-09-07 UTC 登记，在完整回归与留出的任何调用之前写入。

## 依据

修订 12 预先写死的判定是：p27 的可用率不高于 p26 就停止改写线，用 p26 进入完整回归，不做第三次改写。13 批同题同批结果：

| 臂 | 可用率 | 仅格式失败 | 实质失败 | 意图 | 安全违规 | p50 | p95 |
|---|---:|---:|---:|---:|---:|---:|---:|
| p26 | 94.53% | 0.0% | 5.5% | 98.4% | 0 | 1.450 | 1.793 |
| p27 | 92.97% | 0.0% | 7.0% | 98.4% | 0 | 1.429 | 1.832 |

题级配对 64 题 128 对：p27 赢 2 输 4，均值 −0.0156。判定触发，**候选冻结为 p26**。

p27 的两条修改各有得失，一并记录不删：它把 explicit 类从 75% 修到 100%，正是修订 12 想修的 J05；但 action 从 100% 掉到 90%、affect 从 100% 掉到 91.7%、vague 从 100% 掉到 90%。合理的解释是加长的 say 逐字符核对指令挤占了其他行为，但这是推测，四个类别每类只有 10 到 20 题，不作因果结论。J05 的金标准分歧仍然存在且未解决，留在报告里。

## 冻结内容

候选 `prompts/p26_zh.md`，与三个基线臂、两个题集、能力表的哈希一并写入 `final-candidate-freeze.json`。

冻结之后不再改候选提示词。完整回归与留出用同一份文件；若任一环节失败，记录未通过，不回头改 p26 再重跑同一留出。

## 后续两步

- `plans/15_full_regression.json`：134 题开发集 × 中英 × 候选 / v3_inline / v0_contract，804 次。
- `plans/16_holdout.json`：80 题留出 × 中英 × 同三臂，480 次。留出**首次调用后不得再用于调同一候选**。

两个计划都在首次调用前提交。报告按修订 13 的口径，格式合规与任务质量分开列，两个口径都给。
