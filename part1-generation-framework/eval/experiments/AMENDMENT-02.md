# 预登记修订 02：示例勘误与最终候选 p11

记录时间（UTC）：2026-09-07T10:44:08.326Z。p10 三遍语言对照运行中，留出集尚未调用。

1. 静态检查发现 p10 第 13 个示例使用非法时段“早晨”；两种语言均改为词典中的“清晨”。28 个示例（14×2）通过完整结构和能力值校验，中英示例字节相同。
2. p11 同时修正开发阶段已见的规则遗漏：关闭规划中能力仍须警告；不得发明后排声场；精确阈值违反步长时追问，不能擅自近似；观察候选违反全局约束时追问；合法行人警报音色切换不应当作关闭；场景名随 locale。
3. B14/H03 金标准不改，冲突题仍纳入主分母。规范优先可能令任务匹配继续失败，但不得输出非法值来追求分数。
4. p10 已有 A/B、消融和语言数据全部保留，标为开发证据。p11 重做核心 63 题中英三遍语言对照（756 次）与同一 32 题六臂 A/B/消融（384 次），随后冻结策略。
5. 最终全量 134 题中英三遍（804 次）、未见输出的 24 题留出三遍（144 次）、连接复用 144 次、独立模型盲评 24 对保持。预估 DeepSeek 累计 4570 次，把尝试上限调至 5000；余额至少保留 3 元。腾讯端点仍禁用。预算修改在现有运行完成后写入同一累计账本，不重置历史。
6. 一致率预先固定：intent/logic/conditions/actions 三遍完全相同；另报只忽略 AND/OR 条件顺序的指标，保留所有动作顺序，不把金标准可接受的不同方案合并。失败/缺失三遍组计不一致。
7. p11 与 p10 的修改是成组修订，不能单独归因为修正一个示例；p11 的单组件作用仅以其重新运行的单块删除消融讨论。
