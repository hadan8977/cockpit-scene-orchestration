# 预登记修订 01

2026-09-07 18:33 前后，北京时间。已完成1198次官网生成，观察余额12.42元。

- p6因安全回退淘汰；p7仍会直接关闭行人警报音，不冻结。p8编译安全允许值表后，核心集两种语言都零已定义安全违规。
- 04_protocol固定p8中文，只切JSON模式：32对中两组任务通过同为87.5%，JSON同为100%，安全同为0，p95约1.39/1.40秒。样本不足以证明JSON模式提升质量；后续采用它作格式约束。
- p9引入明确locale信封与追问示例；首遍完成252次，后续两遍尚未运行。因英文长名称与观察入口解析问题，停止p9确认，转p10。p9的原始记录全部保留，不能把未运行的两遍视为完成。
- p10只进一步修短名称、观察入口和季节参数缺失的处理；样例与能力表在中英prompt中相同。语言差异由交叉实测判断，不依据单遍结果宣称优越。
- 为保留完整三遍验证，将本轮DeepSeek总尝试上限从3000调整为3500，累计账本不重置，余额仍至少保留3元。不是增加腾讯额度，也不调用腾讯。
- 消融采用固定种子的32题分层开发子集（包含全部8攻击题），四种单块删除加完整对照，共320次；是组件诊断，不能当独立留出。由于词典和最后核对也编码了安全，去掉safety块测的是该块的增量价值，不能称完全取消安全。
- 新留出v2保留24题，在v1基础上为泄露题加入合成标记VIOLET42，并允许小开车窗10%/20%两个有效答案。这些修订没有使用留出模型输出；v1仍保存。最终测试前不运行留出集。
- 另发现H03金标准要求忠实保留两个未发布动作，但全局规则只允许一个，属于既有冲突；同B14一样保留原题与失败，不通过删除题目制造达标。
- 增加统一离线“可用通过率”：原任务通过且名称及回复语言合规，对所有旧新变体同样应用；原始任务通过率仍单列，避免混淆新增口径。

在消融正式执行前加入原p3第六臂：相同32题、相同locale信封/JSON模式/参数，只有系统prompt不同，共增加64次，合计384次。这样可给出干净的prompt A/B，不把先前输入协议与解码配置的变化算作纯prompt收益。总计划仍不超过3500次。另预登记连接复用对照：固定最终prompt、12题中英各3遍、cold/pooled两臂，共144次；只改变HTTP连接复用，两臂均读完SSE响应。它衡量部署网络成本，不归因为prompt改进。
